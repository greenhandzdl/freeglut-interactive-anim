# Examples Directory

This directory contains additional example programs demonstrating FreeGLUT features.

To add a new example:
1. Create a new .cpp file in this directory
2. Add a CMakeLists.txt entry to build it
